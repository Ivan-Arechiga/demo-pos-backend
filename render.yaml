services:
  - type: web
    name: demo-pos-backend
    runtime: docker
    plan: free
    
    # Environment variables
    envVars:
      - key: PORT
        value: "8080"
      - key: SPRING_PROFILES_ACTIVE
        value: default
    
    # Auto-deploy on push to main
    autoDeploy: true
